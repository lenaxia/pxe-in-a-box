---
# ansible-lint configuration for PXE-in-a-Box

exclude_paths:
  - tests/rendered_output/
  - tests/config/

warn_list:
  - experimental
  - jinja[spacing]

skip_list:
  - yaml[line-length]
  - name[casing]

use_default_rules: true
